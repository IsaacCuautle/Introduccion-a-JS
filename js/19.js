//* Comparadores

const numero1 = 20;
const numero2 = "20"

/*
    El operador no estricto == revisa unicamente valores
    El operador estrcito === revisa valor y tipo de dato
*/

if (numero1 === numero2) {true} else {false}