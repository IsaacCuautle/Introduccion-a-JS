//* Operadores ternarios
const money = 400;
const bill = 500;
const card = true;
const auth = true;

// auth ? console.log("autenicado") : console.log("no autenticado");

// money > bill ? console.log("pagado") : console.log("pendiente");

money >= bill || card ? console.log("pagado") :  console.log("pendiente");

//* Ternario anidado
// money >= bill ? console.log("pagado") :  card ? console.log("pagado con cc") : console.log(pendiente);



