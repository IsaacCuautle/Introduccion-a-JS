const sum = (n1, n2) => `Desde sumar: `+ (n1 + n2);

const res = ( n1, n2 ) => `Desde restar: ` + (n1 - n2);

const multiplicar = ( n1, n2 ) => `Desde multiplicar: `+(n1 * n2);


export
{
    sum,
    res
}

export default multiplicar;